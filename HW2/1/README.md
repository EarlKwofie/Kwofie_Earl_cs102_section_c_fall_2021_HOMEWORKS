Rectangle Perimeter Script 

Algorithm/ Synopsis
                
                function perimeter(height, width)
                {
                    string labels[]  = {"Height", "Width"}
                    int height, width
                    
                    for( two iterations )
                    print "Enter the %s newline", label[i]"
                  
                    switch for index of for loop
                    
                      case 0
                      Ask for height input
                      
                      case 1
                      Ask for width input

                      return (2*width) + (2*height);                
                }
